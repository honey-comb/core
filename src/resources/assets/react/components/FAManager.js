import React, {Component} from "react";
import ReactDOM from "react-dom";

export default class FAManager
{
    render ()
    {
        return <script>

        </script>;
    }
}

ReactDOM.render(<FAManager/>, document.body);

